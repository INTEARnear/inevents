[package]
name = "json-filter"
version = "0.1.0"
edition = "2021"
description = "A library for filtering JSON values using a simple query language"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0" 